<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.NFC" />

    <!-- HCE изисква тази feature; required=true означава,
         че приложението не може да се инсталира на устройства без NFC -->
    <uses-feature
        android:name="android.hardware.nfc.hce"
        android:required="true" />

    <application
        android:allowBackup="false"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Student_Attendance_App"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config">
        <!--
            usesCleartextTraffic=true е нужно за HTTP (не HTTPS) в dev среда.
            В продукция: смени SERVER_URL на https:// и премахни това.
        -->

        <!-- Начален екран: Логин -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Главен екран след логин -->
        <activity
            android:name=".HomeActivity"
            android:exported="false" />

        <!--
            HCE Сервис — работи дори когато приложението е на заден план.
            Телефонът отговаря на NFC четеца докато екранът е включен.
        -->
        <service
            android:name=".NfcHceService"
            android:exported="true"
            android:permission="android.permission.BIND_NFC_SERVICE">
            <intent-filter>
                <action android:name="android.nfc.cardemulation.action.HOST_APDU_SERVICE" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
            <meta-data
                android:name="android.nfc.cardemulation.host_apdu_service"
                android:resource="@xml/apduservice" />
        </service>

    </application>
</manifest>