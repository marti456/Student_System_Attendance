<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Student_Attendance_App" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/tu_blue</item>
        <item name="colorSecondary">@color/tu_blue_dark</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/tu_blue</item>
        <!-- Background/Surface -->
        <item name="android:windowBackground">@color/background_light</item>
    </style>

    <style name="Theme.Student_Attendance_App" parent="Base.Theme.Student_Attendance_App" />
</resources>